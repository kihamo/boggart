---
wifi:
  <<: !include ap.yaml
  ssid: !secret zav_wifi_ssid
  password: !secret zav_wifi_password
  domain: ""
  use_address: ${ip_address}
  fast_connect: true # чтобы подключаться к скрытой сети, отключает предварительное сканирование
