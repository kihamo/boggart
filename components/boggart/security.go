package boggart

/*
import (
	"time"

	"github.com/kihamo/go-workers/event"
)

type SecurityStatus int64

const (
	SecurityStatusOpen        SecurityStatus = iota // контур открыт и центр следит за изменениями
	SecurityStatusClosed                            // контур закрыт и центр следит за изменениям
	SecurityStatusOpenForce                         // контур открыт и находится на ручном управлении
	SecurityStatusClosedForce                       // контур закрыт и находится на ручном управлении
)

var (
	SecurityOpen        = event.NewBaseEvent("SecurityOpen")
	SecurityClosed      = event.NewBaseEvent("SecurityClosed")
	SecurityPrepareOpen = event.NewBaseEvent("SecurityPrepareOpen")
)

type SecurityManager interface {
	Status() SecurityStatus
	IsOpen() bool
	IsClosed() bool
	IsForce() bool
	Close()
	Open()
	CloseForce()
	OpenForce()
	PrepareOpen(time.Duration)
}
*/
